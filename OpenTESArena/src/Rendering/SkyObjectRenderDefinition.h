#ifndef SKY_OBJECT_RENDER_DEFINITION_H
#define SKY_OBJECT_RENDER_DEFINITION_H

// Common sky object render data usable by all renderers. Can be pointed to by multiple sky object
// render instances.

class SkyObjectRenderDefinition
{
private:
	// @todo: shared sky object render data a renderer would care about
	// - texture ID
	// - sky object shading type enum (ambient/bright)
public:

};

#endif
