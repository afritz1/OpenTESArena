#ifndef VOXEL_RENDER_INSTANCE_H
#define VOXEL_RENDER_INSTANCE_H

class VoxelRenderInstance
{
private:
	// @todo: unique state values (open door percent, fade percent, etc.)
	// - don't need position because it's defined by the def ID
	int defID;
public:

};

#endif
