#ifndef POP_UP_TYPE_H
#define POP_UP_TYPE_H

// A unique identifier for each kind of pop-up texture.

enum class PopUpType
{
	Dark,
	Parchment,
	Custom1 // Light gray with borders, used for options button.
};

#endif
