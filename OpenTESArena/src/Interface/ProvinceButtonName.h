#ifndef PROVINCE_BUTTON_NAME_H
#define PROVINCE_BUTTON_NAME_H

// A unique identifier for each of the buttons in the province panel.
enum class ProvinceButtonName
{
	Search,
	Travel,
	BackToWorldMap
};

#endif
