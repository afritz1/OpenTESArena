#ifndef SHIELD_ARTIFACT_NAME_H
#define SHIELD_ARTIFACT_NAME_H

// A unique identifier for each artifact that is a shield.
enum class ShieldArtifactName
{
	AurielsShield,
	SpellBreaker
};

#endif
